{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @hub/shared build && pnpm --filter @hub/ai build && pnpm --filter @hub/api prisma:generate && pnpm --filter @hub/api build && pnpm --filter @hub/web build",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "functions": {
    "api/**/*.ts": { "maxDuration": 30 }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
